#	Here are stored all functios for implementing grub-melody program
fusa=16;

parsenota()
{
	nota=`expr &aproximacion \* 
	exit;
}

pedirtempo()
{
	echo $pedirtempo;
	read tempo;
	exit;
}

parsetempo()
{	
	tempo=`expr $tempo \* $fusa \* $aproximacion`; 
	exit;
}

presentacion()
{
	echo $presentacion;
	exit;
}

pedirpartitura()
{
	pedirtempo;
	echo $menu;
	read 
	exit;
}

pedirnombre()
{
	echo $pedirnombre;
	read filename;
	exit;
}
